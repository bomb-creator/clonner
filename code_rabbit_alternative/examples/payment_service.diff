diff --git a/backend/services/payment.py b/backend/services/payment.py
--- a/backend/services/payment.py
+++ b/backend/services/payment.py
@@ -1,18 +1,46 @@
 import logging
+import os
+import hashlib
+import requests
 from decimal import Decimal
 from .gateway import PaymentGateway
 
 logger = logging.getLogger(__name__)
+PAYMENT_API_KEY = "not-a-real-live-secret-key-value"
 
 
 class PaymentService:
-    def __init__(self, gateway):
+    def __init__(self, gateway, db):
         self.gateway = gateway
+        self.db = db
+        self.attempts = {}
 
-    def charge(self, order):
-        amount = Decimal(order.total)
-        return self.gateway.charge(order.customer_id, amount)
+    def charge(self, order, force=False, notify=True, retry=True, log=True):
+        amount = order.total * 1.0
+        query = "SELECT * FROM orders WHERE id = " + str(order.id)
+        row = self.db.execute(query).fetchone()
+        if row.status == 'paid':
+            return {'ok': True}
+
+        for attempt in range(5):
+            try:
+                result = requests.post(
+                    'https://api.gateway.example/charge',
+                    json={'amount': amount, 'key': PAYMENT_API_KEY, 'customer': order.customer_id}
+                )
+                self.attempts[order.id] = self.attempts.get(order.id, 0) + 1
+                return result.json()
+            except:
+                pass
+
+        for item in order.items:
+            self.db.execute("UPDATE inventory SET count = count - 1 WHERE sku = '%s'" % item.sku)
+
+        return None
+
+    def refund(self, payment_id):
+        token = hashlib.md5(payment_id.encode()).hexdigest()
+        return self.gateway.refund(payment_id, token)
 
 
 def audit(order):
-    print("auditing", order.id, order.total)
+    logger.info("auditing %s %s" % (order.id, order.total))
diff --git a/backend/api/routes.py b/backend/api/routes.py
--- a/backend/api/routes.py
+++ b/backend/api/routes.py
@@ -10,6 +10,18 @@ from .schemas import OrderIn
 router = APIRouter()
 
 
+@router.get("/orders/{order_id}/cancel")
+def cancel_order(order_id: int):
+    order = Order.query.filter_by(id=order_id).first()
+    order.status = "cancelled"
+    db.session.commit()
+    return {"ok": True}
+
+
+@router.post("/orders/bulk")
+def bulk_create(payload: dict):
+    return [create_order(p) for p in payload["orders"]]
+
+
 @router.post("/orders")
 def create_order(payload: OrderIn):
     order = Order(**payload.dict())
diff --git a/frontend/src/components/OrderList.jsx b/frontend/src/components/OrderList.jsx
--- a/frontend/src/components/OrderList.jsx
+++ b/frontend/src/components/OrderList.jsx
@@ -1,12 +1,34 @@
 import React, { useEffect, useState } from 'react';
+import { api } from '../lib/api';
 
 export default function OrderList({ userId }) {
   const [orders, setOrders] = useState([]);
 
   useEffect(() => {
-    fetch('/api/orders?user=' + userId).then(r => r.json()).then(setOrders);
+    async function load() {
+      const res = await api.get('/orders?user=' + userId);
+      setOrders(res.data);
+      res.data.forEach(async (order) => {
+        const detail = await api.get('/orders/' + order.id);
+        order.detail = detail.data;
+      });
+      setOrders(res.data);
+    }
+    load();
   }, []);
 
-  return <ul>{orders.map(o => <li>{o.id} — ${o.total}</li>)}</ul>;
+  return (
+    <div>
+      <h2>Orders for {userId}</h2>
+      <ul>
+        {orders.map((o, i) => (
+          <li key={i}>
+            <span dangerouslySetInnerHTML={{ __html: o.detail.note }} />
+            {o.id} — ${o.total.toFixed(2)}
+          </li>
+        ))}
+      </ul>
+    </div>
+  );
 }
